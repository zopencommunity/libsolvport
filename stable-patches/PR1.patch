diff --git a/CMakeLists.txt b/CMakeLists.txt
index 5ba5ed5..88b5d88 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -227,7 +227,7 @@ IF (ENABLE_RPMDB OR ENABLE_RPMPKG_LIBRPM)
   FIND_LIBRARY (RPMDB_LIBRARY NAMES rpmdb)
 
   IF (NOT RPMDB_LIBRARY)
-    FIND_LIBRARY (RPMDB_LIBRARY NAMES rpm)
+    FIND_LIBRARY (RPMDB_LIBRARY NAMES librpm)
   ENDIF (NOT RPMDB_LIBRARY)
 
   FIND_LIBRARY (RPMIO_LIBRARY NAMES rpmio)
diff --git a/ext/solv_xfopen.c b/ext/solv_xfopen.c
index a117a58..fb97519 100644
--- a/ext/solv_xfopen.c
+++ b/ext/solv_xfopen.c
@@ -958,6 +958,32 @@ solv_fmemopen(const char *buf, size_t bufl, const char *mode)
 
 #else
 
+FILE *fmemopen(void *buf, size_t size, const char *mode) {
+    char template[] = "/tmp/zmemXXXXXX";  // mkstemp-compatible
+    int fd = mkstemp(template);
+    if (fd == -1)
+        return NULL;
+
+    // Unlink so it is deleted after close
+    unlink(template);
+
+    FILE *fp = fdopen(fd, "w+b");
+    if (!fp) {
+        close(fd);
+        return NULL;
+    }
+
+    if (buf && size && (strchr(mode, 'r') || strchr(mode, '+'))) {
+        if (fwrite(buf, 1, size, fp) != size) {
+            fclose(fp);
+            return NULL;
+        }
+        rewind(fp);
+    }
+
+    return fp;
+}
+
 FILE *
 solv_fmemopen(const char *buf, size_t bufl, const char *mode)
 {
